"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = status;

var _path = _interopRequireDefault(require("path"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const puppeteerPath = require.resolve('puppeteer-core');

const puppeteerPkg = require(`${_path.default.dirname(puppeteerPath)}/package.json`);

const puppeteerFirefoxPath = require.resolve('puppeteer-firefox');

const puppeteerFirefoxPkg = require(`${_path.default.dirname(puppeteerFirefoxPath)}/package.json`);

async function status() {
  return {
    message: '',
    ready: true,
    puppeteerVersion: puppeteerPkg.version,
    puppeteerFirefoxVersion: puppeteerFirefoxPkg.version
  };
}